<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">

        <item
            android:id="@+id/all_publications"
            android:checked="false"
            android:icon="@mipmap/ic_news"
            android:title="@string/all_publications"/>

        <item
            android:id="@+id/my_publications"
            android:checked="false"
            android:icon="@mipmap/ic_publications"
            android:title="@string/my_publications"/>

        <item
            android:id="@+id/bookmarks"
            android:checked="false"
            android:icon="@mipmap/ic_bookmark_black_24dp"
            android:title="@string/bookmarks"/>
    </group>
</menu>